<?xml version="1.0" encoding="utf-8"?>
<CodeSnippets xmlns="http://schemas.microsoft.com/VisualStudio/2005/CodeSnippet">
  <CodeSnippet Format="1.0.0">
    <Header>
      <Title>Lab 7.7 - Minimalne drzewo rozpinające FULL</Title>
      <Shortcut>alg_graf_mst_77</Shortcut>
      <Description>MST sieci połączeń między miastami + Main + testy | Namespace-ready: wklejaj wewnątrz namespace { }</Description>
      <Author>bearbine</Author>
      <SnippetTypes>
        <SnippetType>Expansion</SnippetType>
      </SnippetTypes>
    </Header>
    <Snippet>
      <Code Language="csharp"><![CDATA[
using System;
using System.Collections.Generic;

public class Edge
{
    public string From;
    public string To;
    public int Weight;

    public Edge(string from, string to, int weight)
    {
        From = from;
        To = to;
        Weight = weight;
    }
}

public class UnionFind
{
    private Dictionary<string, string> parent = new Dictionary<string, string>();

    public void MakeSet(string vertex)
    {
        parent[vertex] = vertex;
    }

    public string Find(string vertex)
    {
        if (parent[vertex] != vertex)
        {
            parent[vertex] = Find(parent[vertex]);
        }

        return parent[vertex];
    }

    public void Union(string a, string b)
    {
        string rootA = Find(a);
        string rootB = Find(b);

        if (rootA != rootB)
        {
            parent[rootB] = rootA;
        }
    }
}

public class Program
{
    static List<Edge> Kruskal(List<string> vertices, List<Edge> edges)
    {
        List<Edge> mst = new List<Edge>();
        UnionFind uf = new UnionFind();

        foreach (string vertex in vertices)
        {
            uf.MakeSet(vertex);
        }

        edges.Sort((a, b) => a.Weight.CompareTo(b.Weight));

        foreach (Edge edge in edges)
        {
            if (uf.Find(edge.From) != uf.Find(edge.To))
            {
                mst.Add(edge);
                uf.Union(edge.From, edge.To);
            }
        }

        return mst;
    }

    public static void Main()
    {
        List<string> miasta = new List<string>() { "A", "B", "C", "D", "E" };

        List<Edge> polaczenia = new List<Edge>()
        {
            new Edge("B", "C", 1),
            new Edge("A", "C", 2),
            new Edge("D", "E", 2),
            new Edge("A", "B", 4),
            new Edge("B", "D", 5),
            new Edge("C", "D", 8),
            new Edge("C", "E", 10)
        };

        List<Edge> mst = Kruskal(miasta, polaczenia);
        int totalCost = 0;

        Console.WriteLine("Krawedzie minimalnego drzewa rozpinajacego:");
        foreach (Edge edge in mst)
        {
            Console.WriteLine(edge.From + " - " + edge.To + ", koszt: " + edge.Weight);
            totalCost += edge.Weight;
        }

        Console.WriteLine("Laczny koszt: " + totalCost);
    }
}
]]></Code>
    </Snippet>
  </CodeSnippet>
</CodeSnippets>
